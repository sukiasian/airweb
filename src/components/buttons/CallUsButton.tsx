import RoundButton from './RoundButton';

const CallUsButton = (): JSX.Element => {
    return <a href="tel:+17473557678">
        <RoundButton classnames="call-us-button">Call Us</RoundButton>
    </a>;
};

export default CallUsButton;
